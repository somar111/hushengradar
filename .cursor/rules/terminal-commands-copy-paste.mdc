---
description: 给用户的终端命令须在新开 Terminal 中可直接复制运行（含 cd 与完整路径）
alwaysApply: true
---

# 终端命令：开箱即用

当用户要在 **Terminal 里自己跑** 的命令时：

1. **假定新开一个 Terminal 标签**，当前目录是 `~` 或任意未知路径，**不要**假设已在项目根目录。
2. **每条命令给全**：需要时在开头写 `cd /Users/apple/hushengradar`（或该仓库绝对路径），再跟 `npm` / `node` / `git` 等；或整条用一行 `cd … && …`。
3. **可复制即用**：不要只写 `npm run db:migrate` 而不带 `cd`；不要省略 `node --env-file=.env.local` 等本项目脚本依赖的前缀。
4. **多步命令**：用完整块列出，每步都能单独粘贴运行；不要写「先 cd 到项目」让用户自己猜路径。
5. 我（Agent）在沙箱里代跑时仍可用相对路径；**写给用户看的命令**遵守上文。
