---
description: 数据库删改与结构变更须谨慎确认
alwaysApply: true
---

# 数据库操作安全

对 Supabase / PostgreSQL 数据集做任何**删改数据**或**结构性改动**（迁移、DROP/ALTER、批量 UPDATE/DELETE、重置水位线等）时：

1. **先想清楚**：影响范围（哪些表、多少行、能否回滚）、是否生产环境、有无更安全的替代方案。
2. **再三确认**：未获用户明确同意前，不执行破坏性或未请求的写操作；连接串、迁移、脚本跑库前说明将做什么，等确认后再执行。
3. **已有数据的库**：不重跑会 DROP 或重建表的初始迁移；只应用增量、幂等变更（如 `ADD COLUMN IF NOT EXISTS`）。
4. **凭证**：不把 connection string / 密码写入仓库或提交 git。
